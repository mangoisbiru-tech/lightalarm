package com.lightalarm.nativeapp.ui.theme

import androidx.compose.ui.graphics.Color

// Primary gradient colors (orange to blue)
val OrangeStart = Color(0xFFFF6600) // Webapp orange
val OrangeEnd = Color(0xFFFFCC00) // Webapp yellow
val BlueStart = Color(0xFF42A5F5) // Light blue
val BlueEnd = Color(0xFF1976D2) // Dark blue

// Background gradient
val BackgroundStart = Color(0xFFE3F2FD) // Very light blue
val BackgroundEnd = Color(0xFFBBDEFB) // Light blue

// Card colors
val CardBackground = Color(0xFFFFFFFF) // White
val CardElevation = Color(0x1A000000) // Light shadow

// Text colors
val PrimaryText = Color(0xFF212121) // Dark gray
val SecondaryText = Color(0xFF757575) // Medium gray
val WhiteText = Color(0xFFFFFFFF) // White

// Accent colors
val AccentBlue = Color(0xFF2196F3) // Material blue
val AccentOrange = Color(0xFFFF9800) // Material orange

// Light theme colors (8 themes) - Matching webapp gradients
val SunriseStart = Color(0xFFFF6600) // Webapp: #ff6600
val SunriseEnd = Color(0xFFFFCC00) // Webapp: #ffcc00

val GreenGrassStart = Color(0xFF00FF00) // Webapp: #00ff00
val GreenGrassEnd = Color(0xFF00CC00) // Webapp: #00cc00

val BlueSeaStart = Color(0xFF00D2FF) // Webapp: #00d2ff
val BlueSeaEnd = Color(0xFF3A7BD5) // Webapp: #3a7bd5

val SephoraBlueStart = Color(0xFF00CED1) // Webapp: #00ced1
val SephoraBlueEnd = Color(0xFF008080) // Webapp: #008080

val PastelGreenStart = Color(0xFF00FF00) // Webapp: #00ff00
val PastelGreenEnd = Color(0xFF00CC00) // Webapp: #00cc00

val AuroraStart = Color(0xFF00FF66) // Webapp: #00ff66
val AuroraEnd = Color(0xFF0099FF) // Webapp: #0099ff (simplified from 3-color gradient)

val PinkOceanStart = Color(0xFFFF1493) // Webapp: #ff1493
val PinkOceanEnd = Color(0xFFFF69B4) // Webapp: #ff69b4

val LavenderStart = Color(0xFF9370DB) // Webapp: #9370db
val LavenderEnd = Color(0xFF7B68EE) // Webapp: #7b68ee

// Additional theme
val AllNightStart = Color(0xFFFFD700) // Webapp: #ffd700
val AllNightEnd = Color(0xFFFFA500) // Webapp: #ffa500

// Toggle colors
val ToggleOn = Color(0xFF4CAF50) // Green
val ToggleOff = Color(0xFFBDBDBD) // Gray

// Progress colors
val ProgressBackground = Color(0xFFE0E0E0) // Light gray
val ProgressFill = Color(0xFFFF9800) // Orange

// Dark theme colors for new design
val DarkBackground = Color(0xFF0F0F0F) // Very dark gray/black
val DarkSurface = Color(0xFF1C1C1C) // Dark surface
val DarkCard = Color(0xFF2A2A2A) // Dark card background
val DarkToggleActive = Color(0xFF4A90E2) // Active toggle color
val DarkToggleInactive = Color(0xFF404040) // Inactive toggle color
val DarkAccent = Color(0xFF6366F1) // Accent color for dark theme
